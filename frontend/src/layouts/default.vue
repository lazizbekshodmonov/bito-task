<script setup lang="ts">
  /**
   * DefaultLayout
   *
   * @description Minimal default layout that renders only the router view.
   *   Used as the base layout for pages that do not require navigation or headers.
   *
   * @usage Applied automatically by the router when no specific layout is specified.
   */
</script>

<template>
  <router-view />
</template>
